{
  "extends": "./tsconfig.app.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.test.tsbuildinfo",
    /* "node" is needed by msw/node; the app project deliberately omits it. */
    "types": ["vite/client", "node"]
  },
  "include": ["src"],
  "exclude": []
}
